"""Module for the maps app configuration."""
from django.apps import AppConfig


class MapsConfig(AppConfig):
    """Configuration for the maps app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "maps"
