"""File is used to configure the app."""
from django.apps import AppConfig


class ColorsConfig(AppConfig):
    """Configuration for the colors app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "colors"
