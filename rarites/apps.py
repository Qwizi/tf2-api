"""File is used to configure the rarites app."""
from django.apps import AppConfig


class RaritesConfig(AppConfig):
    """Configuration for the rarites app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "rarites"
